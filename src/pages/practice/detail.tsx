import React from "react";
import { AppLayout } from "@/components/layout";

const Detail = () => {
  return (
    <AppLayout>
      <div>detail</div>
    </AppLayout>
  );
};

export default Detail;
